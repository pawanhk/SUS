import java.io.IOException;

public class brain {
    public static void main(String[] args) throws IOException {
        // debug settings
        // manual data enter || churner beta

        //collector col = new collector();
        //col.enterData();

        // feed directions/instructions

        //feeder feed = new feeder();
        //feed.setDirections();



        // find and store definitions
        /*
        String def = "language";
        // check if word is already stored
        saveMeanings defs = new saveMeanings(def);
        Boolean stored = defs.checkMeaning();
        if(!stored){
            defs.saveMeanings();
        }else{
            System.out.println("definition already stored ... ");
        }
         */


        //interaction
        //interact inter = new interact();
        //inter.displayMenu();

    }
}
